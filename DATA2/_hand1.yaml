ROOT:
  type: player
player:
  cond:
    instanceof: _player
  type: list
  elm: .hand
# =>nach source & pools werden NUR die players _rsg haben!!!
# aber NICHT die cards c1,c2,c3
# dh brauche:
cards:
  cond:
    obj_type: card
  type: card
  data: .short_name
