defaults:
  type: panel
  panel:
    params:
      bg: red
      fg: white
      orientation: v
      split: equal
  list:
    params:
      bg: yellow
      fg: black
      orientation: h
      split: equal
  info:
    params:
      bg: blue
      fg: white
      font: 16pt Comic Sans MS #AlgerianRegular
      border-radius: 22px
      border: 5px solid red
      # font: italic small-caps bold 12px/24px Georgia, serif
      # font-family: AlgerianRegular
      # font-size: 20px
      # font: italic small-caps bold 12px/30px Georgia, serif