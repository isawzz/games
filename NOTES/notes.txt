

1. nodes ohne cond haben empty pool
=> no contribution to any o._rsg

2. o._rsg empty heisst: dieses object kommt in keinem pool vor!
=> wo kann diese object presented werden?
=> ?




























creating children of elm and panel aehnlich wie creating children of board!

types (currently available)
	container types: panel, list, grid, hand
		=>produce children in rsg tree
	leaf types: info, [dict], card, title, picto, [image]
	

in nodes (start) habe:

type [required! + ROOT MUST BE SINGLE PANEL] (no cond)
	=> standard type or spec node key (forward merging)
	=> string or list* syntax
	
sub|elm (fuer type panel, list|hand resp)
	=> propPath, list or dict syntax
	=> daraus werden children!
	(ausser bei grid, das selbst chi generiert)
	
data (fuer info|dict|card|title|picto)
	=> propPath, list* or dict syntax*
	=> daraus wird content
	
params
	=> visual info
	
cond, _source
	=> cond nur at top level of spec node (not in ROOT)
	=> daraus ergibt sich source, pool, _rsg bei object
	
_id, _ref, 
	=> backward merging
	=> string or list* syntax

position*
	=> loc determined by o, MUST EVAL TO oid of another o!
	=> string or property path 
	
name (hat keine bedeutung oder um nested parts zu id)

in nodes (gen20) habe: 
pool, source			
oid						
specKey

serverData objects haben:
o._rsg liste

TODO in gen20:




























































































